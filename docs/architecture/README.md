# System Architecture & Workflows

Thư mục lưu trữ tài liệu thiết kế kiến trúc hệ thống:
- `system-architecture.md`: Kiến trúc phân tầng backend (Controller - Service - Repository - Validator), mô hình client-server.
- `workflow-state-machine.md`: Máy trạng thái (State Machine) xử lý thành tích và khen thưởng.
- `security-storage.md`: Cơ chế xác thực JWT, HttpOnly Cookie, bảo mật kho lưu trữ minh chứng.
